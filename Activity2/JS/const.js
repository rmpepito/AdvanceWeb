// Create an object:
const car = {type:"Tesla", color:"white"};

// Change a property:
car.color = "black";

// Add a property:
car.owner = "Ronna";

// Display the property:
document.getElementById("const").innerHTML = 
"The car owner is " + car.owner;